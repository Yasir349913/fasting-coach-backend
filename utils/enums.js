const USER_ROLES = {
  ADMIN: 'admin',
  USER: 'user',
};

const PAYMENT_STATUS = {
  PENDING: 'pending',
  PAID: 'paid',
};

const PRICING_TYPE = {
  MONTHLY: 'monthly',
  YEARLY: 'yearly',
};

module.exports = { USER_ROLES, PAYMENT_STATUS, PRICING_TYPE };
